```shell
# 创建一个容器 并让其在后台运行
docker run -d \
    # 给容器命名（唯一、不重复） 
  --name mysql \
    # 端口映射 （宿主机端口: 容器内端口）
  -p 3306:3306 \
    # 环境变量 (不同应用有所不同)
  -e TZ=Asia/Shanghai \
  -e MYSQL_ROOT_PASSWORD=123 \
    # 镜像名字
  mysql:latest
```

## 查看本地镜像

```shell
docker images
```